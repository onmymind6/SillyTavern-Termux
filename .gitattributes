# 强制所有 shell 脚本使用 LF 换行，避免在 Windows 上 checkout 时被转成 CRLF
# 导致 bash 报 "$'\r': command not found"
*.sh    text eol=lf
*.env   text eol=lf

# bash 显式指定
*.bash  text eol=lf

# 默认文本文件交给 Git 自动处理
*       text=auto
